<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'User Profile' %></title>
    <link rel="stylesheet" href="/styles.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="bg-light">
    <%- include('../partials/header') %>

    <main class="container mt-5">
      <div class="card shadow p-4 rounded-3">
        <div class="text-center mb-4">
          <img
            src="https://www.w3schools.com/howto/img_avatar.png"
            alt="User Avatar"
            class="rounded-circle mb-3"
            width="120"
          />
          <h3><%= user.name %></h3>
          <p class="text-muted"><%= user.role?.name || 'User' %></p>
        </div>

        <div class="mb-3">
          <strong>Email:</strong> <%= user.email %>
        </div>
        <div class="mb-3">
          <strong>Created At:</strong> <%= new Date(user.createdAt).toLocaleString() %>
        </div>
        <div class="mb-3">
          <strong>Updated At:</strong> <%= new Date(user.updatedAt).toLocaleString() %>
        </div>

        <div class="text-center mt-4">
          <a href="/api/v1/auth/web/logout" class="btn btn-outline-danger">Logout</a>
        </div>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>
